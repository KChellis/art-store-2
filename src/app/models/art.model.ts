export class Art {
  constructor(public name: string, public type: string, public materials: string[], public price: number, public image: string, public styles: string[], public description: string){}
}
